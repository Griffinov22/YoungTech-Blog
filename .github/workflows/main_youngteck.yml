# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy React app to Azure Web App - YoungTeck

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: "20.x"

      # Change directory to 'frontend' folder, install dependencies, build the app
      - name: npm install, build, and test (frontend)
        run: |
          cd frontend
          npm install
          npm run build --if-present
          npm run test --if-present
        env:
          VITE_CLIENT_ID: ${{ secrets.VITE_CLIENT_ID }}
          VITE_DIRECTORY_ID: ${{ secrets.VITE_DIRECTORY_ID }}
          VITE_BASE_URL: ${{ secrets.VITE_BASE_URL }}
          VITE_RECENT_AMOUNT: ${{ secrets.VITE_RECENT_AMOUNT }}

      # Upload only the build output of the frontend folder for deployment
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: react-app
          path: frontend/dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      id-token: write
    environment:
      name: "production"
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: react-app

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # Deploy only the frontend (React app)
      - name: "Deploy to Azure Web App"
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: "YoungTeck"
          slot-name: "production"
          package: .
          publish-profile: ${{ secrets.AzureAppService_PublishProfile_16c8822a57c94f858dba18271e13cc3f }}
